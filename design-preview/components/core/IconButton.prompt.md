Icon-only button for headers, toolbars and card actions. Always pass `aria-label`.

```jsx
<IconButton aria-label="Menu"><Menu /></IconButton>
<IconButton variant="primary" round aria-label="Appeler"><Phone /></IconButton>
```

Same variants/sizes as `Button`. `round` makes it a circle.
