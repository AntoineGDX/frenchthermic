Small pill for statuses, categories and metadata labels.

```jsx
<Badge variant="teal">Expertise</Badge>
<Badge variant="success" dot>Disponible</Badge>
<Badge variant="accent">Nouveau</Badge>
```

Variants: `neutral`, `teal`, `accent`, `success`, `warning`, `danger`, `solid`. `dot` adds a leading status dot.
